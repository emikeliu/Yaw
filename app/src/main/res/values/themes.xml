<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Yaw" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:activityOpenEnterAnimation">@anim/activity_in</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>

    </style>
    <style name="Popup_Menu" parent="Widget.Material3.PopupMenu"/>
    <style name="Theme.Yaw.Overlay" parent="ThemeOverlay.Material3.DynamicColors.DayNight"/>
    <style name="Popup_Window" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/from_top</item>
        <item name="android:windowExitAnimation">@anim/to_top</item>
    </style>
    <style name="Theme.Yaw.Dark" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:forceDarkAllowed">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowSplashScreenBackground">@color/black</item>
    </style>

</resources>